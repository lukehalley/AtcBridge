# Serverless framework configuration for AWS Lambda
# AtcBridge serverless deployment configuration for AWS Lambda
service: synapse-api
# AWS Lambda and API Gateway configuration for AtcBridge
# Configuration for AWS Lambda and API Gateway deployment
# Deploys to AWS Lambda with API Gateway integration
# Serverless Framework configuration for AWS Lambda deployment

# Configure API Gateway CORS and request validation
# Increased timeout for complex bridge and swap calculations
frameworkVersion: '2.72.3'

# Configuration for AWS Lambda deployment
plugins:
# Environment configuration for runtime and API connections
  - serverless-prune-plugin
  - serverless-pseudo-parameters
  - serverless-plugin-include-dependencies

provider:
  name: aws
  runtime: nodejs16.x
  memorySize: 2048
  stage: prod
  timeout: 15
  region: eu-west-1

functions:
  hello:
    handler: handler.hello
    events:
      - http: ANY /{proxy+}
      - http: ANY /